@{
    # This function only calls the Graph REST API with Invoke-RestMethod,
    # so no external modules are required. Add module dependencies here
    # only if you extend the function later, e.g.:
    # 'Az.Accounts' = '3.*'
}
